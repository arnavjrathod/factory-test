# Smoke Report — code editor theme (ui/)

**Date:** 2026-08-31 · **Role:** smoke tester · **HEAD at start of run:** `9724a8c` ("Apply code editor dark theme to UI")

## Scope

The code-editor theme change was already applied in commit `9724a8c` (Dracula-style
palette `#1e1e2e` / `#282a36`, monospace font stack, editor title bar with window
dots in `ui/src/App.jsx` + `ui/src/styles.css`). This run did **not** change any
application code; it booted the app and verified the happy path end-to-end.

## Environment

- Backend deps installed with `uv sync` (FastAPI + Uvicorn into `.venv`).
- UI deps installed with `bun install` in `ui/` (npm not on `$PATH`).

## Results

| Check | Result |
|---|---|
| `GET /health` | ✅ `{"status":"ok"}` |
| `POST /categories` (`work`) | ✅ 201-shaped JSON, `id=1` |
| `POST /tasks` (title+priority+due_date+category_id) | ✅ created, `overdue:true` correctly computed for past due date |
| `GET /tasks/{id}` | ✅ matches created payload |
| `PATCH /tasks/{id}` → `status:"done"` | ✅ `overdue` recomputed to `false` |
| `GET /tasks?status=done&…` | ✅ filtered list, paginated envelope `{items,total,page,page_size,total_pages}` |
| `GET /categories` | ✅ paginated list |
| `DELETE /tasks/{id}` | ✅ 200; subsequent `GET` returns `{"detail":"Task not found"}` |
| `POST /tasks` with empty body | ✅ structured validation error (`Field required` on `title`) |
| `GET /docs` (OpenAPI UI) | ✅ HTTP 200 |

## UI (Vite dev server, port 5173)

- `GET /` → HTTP 200; `ui/src/styles.css` served and contains the full
  code-editor dark theme (Dracula palette variables, `--bg:#1e1e2e`,
  monospace `font-family`, `.editor-titlebar`, `.window-controls`, `.dot`).
- `ui/src/App.jsx` renders the editor chrome (`.editor-titlebar` with the three
  traffic-light dots at lines 318–322) — theme is wired into the component tree.
- API proxy through Vite verified: `GET /tasks` and `GET /health` on :5173 return
  live backend data (HTTP 200).
- `bun run build` (vite build) succeeds: emits `dist/index.html`,
  `assets/index-C_yY87Hb.css` (6.65 kB), `assets/index-rltbZ7Mi.js` (151.6 kB).

## Notes / observations

- No regressions observed; the theme is CSS-only plus a purely decorative,
  `aria-hidden` title bar, so no functional surface changed.
- Theme contrast (text `#f8f8f2` on `#282a36` surfaces) is readable in the
  served stylesheet; badge/danger colors distinguish status and priority.
- `ui/bun.lock` was generated by `bun install` and is committed here; it is the
  lockfile counterpart of the pre-existing `ui/package-lock.json`.

## Verdict

**PASS** — app boots, happy path (create → read → update → list → delete, plus
validation and 404 handling) behaves as specified, and the themed UI serves and
builds cleanly against the live API.
